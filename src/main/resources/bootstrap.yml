server:
  port: 9265 # config server의 'configClient01-dev.yml' 파일에 server.port없으면 적용
  
spring:
  application:
    name: configClient01 # config server의 'configClient01-dev.yml' 파일명, 'configClient01' 부분 동일
  profiles:
    active: dev # config server의 'configClient01-dev.yml' 파일명에서 참고, 'dev' 부분 동일
    
  cloud:
    config:
      uri: http://localhost:8888 # config server url
